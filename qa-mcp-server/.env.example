# Database connection (required for inspect_db tool)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# DataDog (optional — metrics skip silently when absent)
DD_API_KEY=
DD_SITE=datadoghq.com
