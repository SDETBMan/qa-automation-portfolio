[pytest]
testpaths = evals
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts = -v --tb=short
markers =
    smoke: fast sanity checks — run on every push
    regression: full evaluation suite — run nightly
    safety: bias and toxicity checks
