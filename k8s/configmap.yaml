apiVersion: v1
kind: ConfigMap
metadata:
  name: healenium-config
  namespace: selenium-grid
data:
  # Non-sensitive configuration only. Credentials are in secret.yaml.
  POSTGRES_HOST: postgres-db
  POSTGRES_DB: healenium_db
