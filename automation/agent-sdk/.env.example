# Agent SDK uses Claude Code's existing authentication.
# No API key needed if `claude` CLI is already authenticated.
#
# Optional: override the model used for health assessment
# CLAUDE_MODEL=claude-sonnet-4-20250514
