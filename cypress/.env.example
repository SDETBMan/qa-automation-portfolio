# Cypress TypeScript — environment variables
# Copy this file to .env and fill in values (never commit .env)

# Base URL for SauceDemo (default used if not set)
BASE_URL=https://www.saucedemo.com

# DataDog — optional; metrics are skipped gracefully when absent
DD_API_KEY=
DD_SITE=datadoghq.com
