# conv-eval — Multi-turn conversation evaluation with DeepEval
OPENAI_API_KEY=sk-...           # Required — OpenAI API key for chatbot and evaluations
DD_API_KEY=                      # Optional — DataDog CI Visibility metrics (skipped if absent)
DD_SITE=datadoghq.com           # Optional — DataDog regional site
