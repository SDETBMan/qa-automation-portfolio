# Self-Healing backend location
heal-enabled=true
hlm.server.url=http://localhost:7878
hlm.imitator.url=http://localhost:8000

# 60% similarity is enough to heal a broken locator
recovery-tries=1
score-cap=0.6
