# failure-triage — Claude-powered test failure root-cause analysis
ANTHROPIC_API_KEY=sk-ant-...    # Required — Claude API key for triage agent
DD_API_KEY=                      # Optional — DataDog metrics (skipped if absent)
DD_SITE=datadoghq.com           # Optional — DataDog regional site
